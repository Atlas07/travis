language: node_js

node_js:
  - stable

branches:
  only:
    - master

services:
  - docker

before_install:
  # - docker build -t "travis" .
  # - docker run -d -p 8080:8080 travis
  - docker-compose up -d
  - docker ps -a

deploy:
  provider: heroku
  api_key:
    secure: "392eac4c-63bd-4746-b8e9-dfbcfce795e0"
    
  
# script:
#   - npm start